# Core Tuner Magisk Module

This module should be used together with the **Core tuner** app to apply persistence on boot

Este módulo deve ser usado em conjunto com o app **Core Tuner** para aplicar persistência no boot.

This module is still in development

Este módulo ainda está em desenvolvimento